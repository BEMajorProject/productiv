import MainHoc from 'renderer/components/MainHoc/MainHoc';
import NotesManager from 'renderer/components/NotesManager/NotesManager';

const Notes = () => {
  return <MainHoc component={NotesManager} />;
};

export default Notes;
